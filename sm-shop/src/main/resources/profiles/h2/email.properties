# Email Configuration (Local Development - Disabled)
# For local dev, email is optional

mailSender.protocol=smtp
mailSender.host=localhost
mailSender.port=25
mailSender.username=
mailSender.password=
mailSender.starttls=false
mailSender.auth=false

# Set to false to disable email in local dev
email.active=false
